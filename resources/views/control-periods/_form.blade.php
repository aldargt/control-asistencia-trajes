@php($editing = isset($controlPeriod))
<div class="grid gap-5 sm:grid-cols-3">
    <div><label for="month" class="mb-2 block text-sm font-medium">Mes <span class="text-red-600">*</span></label><select id="month" name="month" required class="field @error('month') field-error @enderror">@foreach(range(1, 12) as $month)<option value="{{ $month }}" @selected((int) old('month', $controlPeriod->month ?? today()->month) === $month)>{{ ucfirst(\Illuminate\Support\Carbon::create(2000, $month)->locale('es')->translatedFormat('F')) }}</option>@endforeach</select>@error('month')<p class="field-message">{{ $message }}</p>@enderror</div>
    <div><label for="year" class="mb-2 block text-sm font-medium">Año <span class="text-red-600">*</span></label><input id="year" name="year" type="number" min="2000" max="2100" value="{{ old('year', $controlPeriod->year ?? today()->year) }}" required class="field @error('year') field-error @enderror">@error('year')<p class="field-message">{{ $message }}</p>@enderror</div>
    <div><label for="reference_days" class="mb-2 block text-sm font-medium">Días de referencia <span class="text-red-600">*</span></label><input id="reference_days" name="reference_days" type="number" min="1" max="31" value="{{ old('reference_days', $controlPeriod->reference_days ?? config('hour-control.default_monthly_reference_days')) }}" required class="field @error('reference_days') field-error @enderror">@error('reference_days')<p class="field-message">{{ $message }}</p>@enderror</div>
</div>
<div class="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900"><strong>Base de cálculo:</strong> los días de referencia permiten convertir las horas semanales de cada condición en horas esperadas para este período. No representan días obligatorios de asistencia ni modifican el salario acordado.</div>
<div class="mt-8 flex flex-col-reverse gap-3 border-t border-slate-200 pt-6 sm:flex-row sm:justify-end"><a href="{{ route('control-periods.index') }}" class="button-secondary">Cancelar</a><button type="submit" class="button-primary">{{ $editing ? 'Guardar cambios' : 'Crear período' }}</button></div>
